# Validation: Admin Recovery

**Result**: PASS técnico local — 7/7 critérios, 289 testes e sensor comportamental 7/7 detectado.

**Data**: 2026-09-07. **Verificador**: subagente independente, sem autoria da implementação. Escopo: incremento de recuperação administrativa; API, worker, domínio, contratos e interface. Somente este relatório foi editado pelo verificador nesta rodada. O WIP anterior foi preservado; não houve chamadas a providers, uso do banco operacional, commit ou deploy.

## Critérios e evidências

| AC     | Resultado esperado e evidência com assertion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Resultado |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| OPS-01 | Falhas atuais aparecem mesmo em pedidos entregues, sem históricos resolvidos: `apps/api/src/admin-recovery.test.ts:669` exige `list.total === 3`, linha670 compara os três IDs e linha676 exige contador anterior +3. Linha682 cobre estados terminais. Linha596 exige `errorCode === 'provider_limit'`; linha599 impede vazamento do segredo sintético; linhas615–625 verificam diagnóstico anterior, ator e horário nas notas. `apps/web/src/admin/routes.test.tsx:142` exige consulta `?attention=failures&page=1`; linha445 inicia sequências temporais ativo→terminal, linha459 cobre erro transitório e linha478 sessão expirada.                             | PASS      |
| OPS-02 | Retry somente para falha elegível: `apps/api/src/admin-recovery.test.ts:181` verifica pagamento, job ativo e inexistente com 409/404. Linha342 cobre concorrência; linha353 exige respostas `[200,409]` e preservação das variantes. Linha284 verifica que o mesmo job passa de attempts6/max6 para pending6/max7, sem zerar tentativas, e o segundo retry retorna409. A mutação que ignora capability produz `[200,200]` e é detectada.                                                                                                                                                                                                                            | PASS      |
| OPS-03 | Pagamento e aprovação são gates independentes: `apps/api/src/admin-recovery.test.ts:476` testa unpaid-approved e paid-unapproved, exigindo409 sem alterar job. Linha379 regenera uma variante e linha382 exige que a parceira permaneça; linha390 exige payload `{variant:1}` e maxAttempts1. `apps/worker/src/flow.test.ts:472` cobre retomada apenas da variante faltante; linha1295 exige uma chamada e linha1302 preserva a parceira no dispatch real do worker com provider substituído. `apps/web/src/admin/order-recovery.test.tsx:178` distingue uma faixa do conjunto; confirmação e custo são apresentados por `apps/web/src/admin/order-operations.tsx`. | PASS      |
| OPS-04 | `apps/api/src/admin-recovery.test.ts:210` exige edição histórica não aprovada e retorno a lyrics_ready antes do pagamento. Linha402 cobre letra anteriormente aprovada e bloqueio em payment_pending; linha400 impede edição durante job ativo. Linha516 exige limite4, zero chamadas adicionais e capabilities distintas para gerar/editar. Linhas582–585 preservam a versão antiga e aprovam a nova edição no contexto pago; segurança usa o texto canônico efetivamente editado. As regressões de geração/refino, autorização e privacidade também executaram na suíte API completa.                                                                             | PASS      |
| OPS-05 | `apps/api/src/admin-recovery.test.ts:256` exige409 sem nova referência. Linha284 cobre multipart com arquivo primeiro, bytes inválidos sem perder tentativa, foto válida com consentimento, estado pending e referência nova válida mesmo com tentativa antiga. Linha682 exige capability negada e409 para cancelled/refunded/payment_pending. `apps/worker/src/flow.test.ts:1170` exige rejeição e zero chamadas de capa quando a referência esperada desapareceu. `apps/web/src/admin/order-recovery.test.tsx:112` exige arquivo+consentimento; linhas134/142 mantêm disabled apesar do upload quando a capability é negada ou o job não existe.                  | PASS      |
| OPS-06 | `apps/api/src/admin-recovery.test.ts:432` cobre exclusão mútua entre job legado e notificação, concorrência `[200,409]`, job deliver-notify, envio já concluído sem efeito e acesso revogado bloqueado. `apps/worker/src/flow.test.ts:1212` rejeita áudio incompleto, envia uma vez em duas execuções e preserva os registros de áudio; a suíte de intenção em linha901 cobre também acesso expirado. `apps/web/src/admin/order-recovery.test.tsx:173` exige somente adminRetryEmail, com zero chamadas de geração/rebuild/retry genérico.                                                                                                                          | PASS      |
| OPS-07 | `apps/web/src/admin/order-recovery.test.tsx:63` exige zero geração antes de confirmar e linha70 exatamente uma chamada após confirmação; linha94 mantém erro visível. Linha155 bloqueia confirmação obsoleta, e linha160 bloqueia ações com edição não salva. `apps/web/src/admin/order-operations.test.tsx` cobre impacto, cancelamento e pending disabled. `apps/web/e2e/admin-recovery.spec.ts:69` cobre edição/retomada e atualização; linha123 cobre foto/consentimento e aviso isolado. Efeitos de banco, concorrência e ausência de geração indevida foram executados pelo verificador no PostgreSQL descartável.                                            | PASS      |

## Gates e integridade

- `pnpm check`, Node22, com DATABASE_URL e DATABASE_URL_TEST apontando exclusivamente para `music_admin_recovery_verify`: **exit0**, formato, lint, typecheck, testes e build PASS. Total **289/289**, sem testes pulados: API100, web107, worker45, contracts17, domain11 e providers9. Todas as tarefas de teste registram `cache bypass, force executing`; `turbo.json` desativa cache de testes. Alguns gates estáticos/build reutilizaram cache válido. Evidência: `output/admin-recovery/verifier-check.log`.
- A primeira tentativa parou no formato enquanto o frontend ainda finalizava a formatação; foi preservada em `/tmp/music-admin-recovery-verifier-check-format-before.log`. Após congelamento e as duas últimas correções de texto, o comando normal completo acima passou. O sensor só começou depois desse PASS.
- Baseline integrado anterior: **247**, histórico de lyrics-production-polish. O aumento acumulado é42, incluindo oito testes do follow-up temporal e34 desta entrega. Não houve baseline integrada fresca de255; não se atribui a ela um gate que não foi executado. Não foi identificada redução de testes ou enfraquecimento de assertions na superfície revisada.
- Frente frontend: **48 E2E PASS**, React Doctor **100/100** em52 arquivos, typecheck/lint/format PASS. Execução atribuída ao autor, não ao verificador. Os dois últimos ajustes de copy foram seguidos por29 testes focais e formato PASS, e estão incluídos nos289 independentes. Logs: `output/admin-recovery/web-{e2e-final,doctor,type,lint,format-final,copy-unit,copy-format}.log`.
- Browser real executado pelo principal: contador de duas falhas, filtro/lista correspondentes, pedido saudável ausente, diagnóstico e tentativas do pedido com falha; confirmações de retomar/refazer abertas e canceladas. Evidências: `output/admin-recovery/failure-list.png` e `output/admin-recovery/order-diagnostics.png`. Nenhuma operação paga foi disparada nesse smoke.

## Sensor de discriminação

Sete mutações apenas em cópias scratch, com banco descartável exclusivo e dependências locais. Cada falha foi uma assertion comportamental, sem erro de importação ou sintaxe. **7 killed, 0 survived**. O `git status --porcelain` real ficou idêntico antes/depois e o scratch foi removido. Evidência detalhada: `output/admin-recovery/verifier-sensor/results.json` e logs adjacentes.

| Mutação                                          | Local                                      | Falha discriminante                                               |
| ------------------------------------------------ | ------------------------------------------ | ----------------------------------------------------------------- |
| Ignorar capability no retry                      | `apps/api/src/app.ts:1859`                 | Concorrência retorna200/200 em vez de200/409; nove testes falham. |
| Ignorar pagamento para áudio                     | `apps/api/src/recovery.ts:37`              | Pedido unpaid-approved retorna200 em vez de409.                   |
| Apagar também a variante parceira                | `apps/api/src/app.ts:2148`                 | Registros restantes ficam vazios em vez de preservar a parceira.  |
| Gerar capa sem referência esperada               | `apps/worker/src/worker.ts:979`            | Provider é chamado uma vez em vez de zero.                        |
| Enviar aviso sem par de áudios                   | `apps/worker/src/worker.ts:1083`           | Promise resolve em vez de rejeitar.                               |
| Aprovar implicitamente edição antes do pagamento | `apps/api/src/app.ts:2214`                 | approved_at recebe Date em vez de null.                           |
| Upload superar capability negada                 | `apps/web/src/admin/order-recovery.tsx:68` | Botão deixa de permanecer disabled.                               |

## Revisão e limites

As correções verificadas incluem capability consistente entre DTO e endpoint; lock por pedido; gates financeiros independentes; exclusão de qualquer job ativo em recuperação de áudio; preservação histórica; upload na ordem enviada pelo browser; TTL da nova referência baseado no arquivo; sanitização de diagnóstico; e separação da notificação legada da produção. A implementação mantém fila, providers e histórico existentes, sem migração ou mecanismo paralelo de recuperação. Não restou lacuna concreta nos sete critérios locais.

Este PASS cobre comportamento técnico local. Mocks de provider, E2E e smoke administrativo não certificam geração paga, entrega externa de e-mail, qualidade artística, CI remoto, Railway ou produção. Aceite humano permanece separado. O banco descartável foi removido após os testes.
