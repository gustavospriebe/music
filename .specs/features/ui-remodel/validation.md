# ui-remodel Validation (second pass)

> Registro histórico de um recorte anterior. O modelo vigente e a evidência atual estão em [docs/documentation-map.md](../../../docs/documentation-map.md) e [audit-remediation](../audit-remediation/spec.md). Este registro não autoriza publicação nem gasto com providers.

**Date**: 2026-09-06
**Spec**: `.specs/features/ui-remodel/spec.md` (31 requirements)
**Diff range**: uncommitted worktree vs `HEAD` (`c9f3d04`); feature surface scoped to `apps/web/src/components.tsx`, `apps/web/src/pages/public.tsx`, `apps/web/src/pages/landing.tsx`, `apps/web/src/types.ts`, `apps/web/src/api.ts`, `apps/web/src/styles.css`, `apps/web/src/admin/labels.ts` (new), `apps/web/src/admin/shell.tsx` (new), `apps/web/src/admin/routes.tsx`, `apps/api/src/app.ts`, plus tests `apps/web/src/components.test.tsx`, `apps/web/src/public-form.test.tsx`, `apps/web/src/pages/checkout.test.tsx` (new), `apps/web/src/admin/routes.test.tsx` (new), `apps/api/src/flow.test.ts`, `apps/web/e2e/mvp-flows.spec.ts`, `apps/web/e2e/mvp-operable-t5.spec.ts`, `apps/web/e2e/mvp-operable-t6.spec.ts`, `apps/web/e2e/mvp-operable-t7.spec.ts`, `apps/web/e2e/ui-remodel-qa.spec.ts` (new)
**Verifier**: independent sub-agent, second pass (author ≠ verifier)
**First report**: `.specs/features/ui-remodel/validation.md` v1 — FAIL 16/31, sensor 3/4 (F-01..F-05 + F-S2). This report re-derives everything independently; conclusions are not inherited.
**Result**: FAIL

Pre-existing WIP explicitly out of scope and untouched by this verification: `apps/worker/src/worker.ts` (import reorder), plus older modified docs/config (`.env.example`, `ci.yml`, `package.json`, `pnpm-lock.yaml`, `ARCHITECTURE.md`, etc.). Porcelain baseline captured before the sensor and re-verified identical after it.

---

## Task Completion

| Task | Status     | Notes                                                                                                                                                                                                          |
| ---- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1   | ✅ Done    | Menu `dialog` + `inert` + trap + Escape in `apps/web/src/components.tsx:19-122`; unit + E2E cover it                                                                                                           |
| T2   | ✅ Done    | `GET /api/v1/admin/overview` (`apps/api/src/app.ts:1311-1341`) + `total`/`pageSize: 30` on `GET /api/v1/admin/orders` (`apps/api/src/app.ts:1281-1309`); integration test added                                |
| T3   | ✅ Done    | `payment: { configured, devFallback }` on `GET /api/v1/orders/:publicId` (`apps/api/src/app.ts:901-905`); web type `apps/web/src/types.ts:42`; integration assertion added                                     |
| T4   | ✅ Done    | `JourneySteps`, CTA fiel, autocomplete, rascunho explicado, preparação (`apps/web/src/pages/public.tsx`); unit + E2E assertions extended                                                                       |
| T5   | ✅ Done    | Checkout resumo/estado/pendências, MyOrders, landing honesta (`apps/web/src/pages/public.tsx`, `apps/web/src/pages/landing.tsx`); unit (`checkout.test.tsx`) + E2E extended                                    |
| T6   | ✅ Done    | `AdminShell` (`apps/web/src/admin/shell.tsx`), labels PT (`apps/web/src/admin/labels.ts`), dashboard alerts-first via overview (`apps/web/src/admin/routes.tsx:124-303`)                                       |
| T7   | ✅ Done    | Explicit filters + pagination (`apps/web/src/admin/routes.tsx:316-465`); unit covers apply/page-reset/total/empty/error                                                                                        |
| T8   | ✅ Done    | Sectioned detail + masked PII + `ConfirmAction` (`apps/web/src/admin/routes.tsx:476-663`); retry + approve + rebuild confirm-execution now all asserted                                                        |
| T9   | ✅ Done    | 44px/focus/overflow styles (`apps/web/src/styles.css`) + E2E (`ui-remodel-qa.spec.ts`, extended `mvp-*`); admin overflow measured; keyboard-only journey partially covered (first step only — see A11Y-07 gap) |
| T10  | ✅ Done    | `local-validation.md` gates/QA/screenshots evidence present; verifier re-ran gates independently (see Gate Check; E2E trusted via file existence + count, not re-executed)                                     |
| T11  | ⚠️ Partial | Report/docs/STATE updated (`docs/ui-remodel-report.md`, `README.md`, `docs/project-context.md`, `.specs/STATE.md`); this validation report is the pending close-out item                                       |

---

## Spec-Anchored Acceptance Criteria

Strict evidence-or-zero: the cited assertion's **asserted value** must equal the spec-defined outcome. Partial coverage is marked GAP (not silently passed).

| Criterion (WHEN X THEN Y)                                                                                                                                                                                                     | Spec-defined outcome                                              | `file:line` + assertion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Result                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| JOURNEY-01: `/criar`, `/criar/letra`, `/criar/checkout` show "Etapa N de 5" (N=1,2,3; História, Letra, Pagamento)                                                                                                             | literal "Etapa 1/2/3 de 5" + step names + `aria-valuenow`         | `apps/web/src/components.test.tsx:108-120` — `it.each([[1,'História'],[2,'Letra'],[3,'Pagamento'],[4,'Produção'],[5,'Entrega']])` renders `<JourneySteps step>` and asserts `getByRole('progressbar',{name:\`Etapa ${step} de 5: ${name}\`})`+`aria-valuemin='1'`+`aria-valuemax='5'`+`aria-valuenow=String(step)`; `apps/web/e2e/ui-remodel-qa.spec.ts:138` `progressbar /etapa 1 de 5: história/`, `:140` `/etapa 2 de 5: letra/`, `:147` `/etapa 3 de 5: pagamento/`; impl `apps/web/src/components.tsx:178-196`                               | ✅ PASS — F-01 closed (names + values for 1-5, E2E pins 1-3 with names)                                                                                                                                                                                                                    |
| JOURNEY-02: submit uses "Salvar história e continuar", navigates to letra step without claiming lyrics generated                                                                                                              | button label + navigation to `/criar/letra`                       | `apps/web/e2e/mvp-flows.spec.ts:82-83` — `click(button /salvar história e continuar/)` then `toHaveURL(/\/criar\/letra\?pedido=/)`; `apps/web/e2e/mvp-operable-t5.spec.ts:55` — same URL assertion                                                                                                                                                                                                                                                                                                                                                | ✅ PASS                                                                                                                                                                                                                                                                                    |
| JOURNEY-03: `story_completed` w/o lyrics shows prep (what generated, expected time, persistence after close, recovery via order link)                                                                                         | 4 prep facts                                                      | `apps/web/e2e/ui-remodel-qa.spec.ts:141-145` — `heading /antes de criar a letra/` + `text /alguns minutos e consome uma tentativa de geração/` (time) + `text /história continua salva/` (persistence) + `text /mesmo navegador ou pela página do pedido/` (recovery) + `button /criar letra agora/`; impl `apps/web/src/pages/public.tsx:442-460` (4 `<li>` bullets)                                                                                                                                                                             | ❌ GAP — 3/4 bullets asserted; bullet 1 "Vamos transformar sua história salva em uma letra revisável em português" (`public.tsx:445`, the what-generated fact) has no assertion in any unit/E2E file (grep for `transformar sua história` = 0 hits)                                        |
| JOURNEY-04: `lyrics_ready` preserves edit, save-as-new-version, approval before payment; `lyrics_generating` announces via `role="status"` + polls; invalid states show readable error without offering generate/edit/approve | edit/save/approve + `role="status"` polling + invalid-state guard | `apps/web/e2e/mvp-operable-t6.spec.ts:88-104` — `status 'Nova versão salva.'`; `:22-50` — `status 'Criando sua letra'` + `generates === 0` after reload (polling, no repeat POST); `:140-163` — inconsistent status shows `alert /estado inconsistente/` + `main buttons count 0`                                                                                                                                                                                                                                                                 | ✅ PASS (pre-existing tests, still green; behavior unchanged by remodel)                                                                                                                                                                                                                   |
| LIB-01: `/minhas-musicas` shows honoree/title, occasion, creation date, progress, next action; no tech ID as title                                                                                                            | 5 fields + no-ID title                                            | `apps/web/e2e/mvp-flows.spec.ts:172-174` — `heading /bia/` + `text /aniversário/` + `text /continuar criação/.first()`; impl `apps/web/src/pages/public.tsx:849-876` (`h2` title/subject, `Ocasião`, `Criada em`, `Progresso`, `Próxima ação`)                                                                                                                                                                                                                                                                                                    | ❌ GAP — title/occasion/next-action asserted; creation date (`Criada em`), progress (`Progresso`), and no-ID-as-title have no assertion; no `MyOrders` unit test (grep `MyOrders\|Criada em\|Progresso` in `*.test.tsx` = 0 hits). Not covered by F-01..F-S2, still open                   |
| FORM-01: name/email render `autocomplete="name"` / `"email"`                                                                                                                                                                  | exact attribute values                                            | `apps/web/src/public-form.test.tsx:85-92` — `toHaveAttribute('autocomplete','name')` / `('autocomplete','email')`                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ PASS                                                                                                                                                                                                                                                                                    |
| FORM-02: form explains draft is this-browser/device only + "Apagar rascunho" clears local storage w/o affecting saved orders                                                                                                  | explanation text + local clear                                    | `apps/web/src/public-form.test.tsx:97-100` — `text /rascunho fica salvo somente neste navegador/` + `click /apagar rascunho/` then `getItem(...) toBeNull()`                                                                                                                                                                                                                                                                                                                                                                                      | ✅ PASS                                                                                                                                                                                                                                                                                    |
| DEMO-01: with no authorized demo media, honest block without `<audio>`, play button, or sound-proof promise                                                                                                                   | no `<audio>`/play + honest copy                                   | `apps/web/e2e/ui-remodel-qa.spec.ts:212-214` — `text /sem áudio demo automático/ toBeVisible()` + `locator('main audio') toHaveCount(0)` + `getByRole('button',{name:/ouvir\|reproduzir\|play demo/}) toHaveCount(0)`; impl `apps/web/src/pages/landing.tsx:30-39`                                                                                                                                                                                                                                                                                | ✅ PASS — F-03 closed (audio count 0 asserted)                                                                                                                                                                                                                                             |
| CHECK-01: checkout with approved order shows product, honoree/approved-title, BRL price, package contents, next step                                                                                                          | 5 summary fields                                                  | `apps/web/src/pages/checkout.test.tsx:59-64` — `heading /resumo do pedido/` + `text 'Música da Resenha'` (product) + `text 'A Resenha da Bia'` (approved title) + `text /R\$\s*49,90/` + `text /duas versões de áudio e página privada/` (contents) + `text /pagamento e depois produção do áudio/` (next step); `apps/web/e2e/ui-remodel-qa.spec.ts:148-153` — same 5 values via `getByText`                                                                                                                                                     | ✅ PASS — F-02 (checkout part) closed                                                                                                                                                                                                                                                      |
| CHECK-02: checkout explains redirect to Mercado Pago                                                                                                                                                                          | redirect copy                                                     | `apps/web/src/pages/checkout.test.tsx:65` — `text /redirecionado ao mercado pago/`; `apps/web/e2e/mvp-operable-t5.spec.ts:84` — same text                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ PASS                                                                                                                                                                                                                                                                                    |
| CHECK-03: unconfigured provider without dev fallback shows unavailability pre-click, keeps button disabled with accessible reason                                                                                             | disabled button + reason                                          | `apps/web/src/pages/checkout.test.tsx:71-80` — `button /pagar com mercado pago/ toBeDisabled()` + `findByRole('status') toHaveTextContent(/pagamento indisponível/)`; `:82-89` — `payment:{configured:false,devFallback:true}` keeps button enabled (branch contrast); `apps/web/e2e/mvp-operable-t5.spec.ts:137-140` — `button toBeDisabled()` + `status /pagamento indisponível/`; `apps/api/src/flow.test.ts:1140-1142` — `toMatchObject({payment:{configured:false,devFallback:true}})`; impl `apps/web/src/pages/public.tsx:553-554,594-612` | ✅ PASS — F-S2 closed; sensor S2 now killed at unit level (see Sensor)                                                                                                                                                                                                                     |
| CHECK-04: unapproved legal/commercial terms show "A definir antes do lançamento", launch stays explicitly blocked, no invented policy                                                                                         | section + blocked-launch statement                                | `apps/web/src/pages/checkout.test.tsx:66-67` — `text /a definir antes do lançamento/` + `text /lançamento comercial segue bloqueado/`; `apps/web/e2e/ui-remodel-qa.spec.ts:155-156` — same two texts; impl `apps/web/src/pages/public.tsx:615-621`                                                                                                                                                                                                                                                                                                | ✅ PASS — F-02 (blocked sentence) closed                                                                                                                                                                                                                                                   |
| ADMIN-01: shell persists across `/admin`, `/admin/pedidos`, `/admin/pedidos/:orderId` with Visão geral/Pedidos nav, operational context, exit                                                                                 | persistent nav on 3 routes + context + exit                       | `apps/web/src/admin/routes.test.tsx:98-144` — loops `['/admin','/admin/pedidos','/admin/pedidos/internal-id-1']`, each `findByRole('navigation',{name:'Administração'}) toBeVisible()` + `a[href="/admin"]` + `a[href="/admin/pedidos"]` not null; `apps/web/e2e/ui-remodel-qa.spec.ts:221-223,240-245,251-256` — `link 'Visão geral'` + `link 'Pedidos'` visible on all 3 routes; impl `apps/web/src/admin/shell.tsx:32-43`                                                                                                                      | ❌ GAP (partial) — nav persistence on 3 routes now asserted; operational context (`shell.tsx:42` `Operação · dados sintéticos fora de produção`) and exit (`shell.tsx:7-16` `Sair` button) have no assertion in any unit/E2E file (grep `Sair\|sintéticos` in `*.test.tsx`/`e2e` = 0 hits) |
| ADMIN-02: technical events/statuses translated to operational PT                                                                                                                                                              | PT labels, no raw English                                         | `apps/web/src/admin/routes.test.tsx:42-43` — `statusPt('review_required') toBe 'Aguardando revisão'`, `eventPt('checkout_started') toBe 'Pagamento iniciado'`; `:95` — `queryByText(/order_created/) not.toBeInTheDocument()`                                                                                                                                                                                                                                                                                                                     | ✅ PASS                                                                                                                                                                                                                                                                                    |
| ADMIN-03: dashboard shows alerts + action-required orders with next decision BEFORE secondary metrics                                                                                                                         | alerts-first DOM order                                            | `apps/web/src/admin/routes.test.tsx:83-88` — `heading /precisam de decisão/ toBeVisible()` + `alertsHeading.compareDocumentPosition(cards) & FOLLOWING` (alerts section precedes `.cards`); `apps/web/e2e/ui-remodel-qa.spec.ts:224-235` — same DOM-order check via `compareDocumentPosition`; impl `apps/web/src/admin/routes.tsx:152-208` (alerts `<section>`) before `:209` (`.cards`)                                                                                                                                                         | ✅ PASS — gap from v1 (order unasserted) now closed even though not in F-list                                                                                                                                                                                                              |
| ADMIN-04: orders/revenue use server aggregates (`GET /api/v1/admin/overview`), never first-page-derived                                                                                                                       | server totals                                                     | `apps/web/src/admin/routes.test.tsx:92-93` — `text /120/`, `text /R\$\s*1\.996,00/` from overview mock (not items); `apps/api/src/flow.test.ts:1105-1120` — `totals.paid/revenueCents toBe SQL count/sum`                                                                                                                                                                                                                                                                                                                                         | ✅ PASS                                                                                                                                                                                                                                                                                    |
| ADMIN-05: list shows pagination with current page, total, empty/error states via API `total`/`pageSize`                                                                                                                       | page/total/empty/error                                            | `apps/web/src/admin/routes.test.tsx:193` — `text /página 1 de 3/` (61 items / 30); `apps/api/src/flow.test.ts:1128-1132` — `page toBe 1`, `pageSize toBe 30`, `total >= 2`; `:146-156` — empty mock → `text /nenhum pedido encontrado para os filtros atuais/` + rejected mock → `text /sessão administrativa inválida/`; `:158-170` — overview rejection → same error, no estimated totals                                                                                                                                                       | ✅ PASS — F-04 (empty/error part) closed                                                                                                                                                                                                                                                   |
| ADMIN-06: any filter change waits for "Filtrar" (no per-key fetch), restarts at page 1                                                                                                                                        | explicit apply + page reset                                       | `apps/web/src/admin/routes.test.tsx:196-204` — `adminOrders toHaveBeenCalledTimes(1)` initially, still 1 after typing/select, 2 after `click /filtrar/` with `toContain('status=failed')` + `toContain('page=1')`                                                                                                                                                                                                                                                                                                                                 | ✅ PASS                                                                                                                                                                                                                                                                                    |
| ADMIN-07: detail shows readable sections (story, lyrics, payment, costs, queue, audio), not raw JSON                                                                                                                          | sections, no `JSON.stringify`                                     | `apps/web/src/admin/routes.test.tsx:246-255` — `heading /história/`, `/pagamento/`, `/custo de ia/`, `/fila e produção/`, `/áudios/` visible + `body.textContent not.toMatch(/externalId\|req /)` + `body.innerHTML not.toMatch(/JSON\.stringify/)`; `apps/web/e2e/ui-remodel-qa.spec.ts:257-264` — `heading /história \(resumo operacional\)/` + absences of full email/internal-id/`externalId`/`JSON.stringify`; impl `routes.tsx` has zero `JSON.stringify`                                                                                   | ❌ GAP (partial) — 5/6 sections + no-JSON asserted; `Letra aprovada` section heading (`routes.tsx:554`) has no role-based assertion in unit or E2E                                                                                                                                         |
| ADMIN-08: PII masked/minimized; no internal IDs, tokens, `externalId`, unnecessary personal payloads                                                                                                                          | masked email + absences                                           | `apps/web/src/admin/routes.test.tsx:48-52` — `maskEmail('ana@example.test') toBe 'a•••@example.test'` + `not.toContain('ana@')`; `:247-250` — masked visible, full email/internal-id absent, body `not.toMatch(/externalId/)`; `apps/web/e2e/ui-remodel-qa.spec.ts:260-263` — same absences                                                                                                                                                                                                                                                       | ✅ PASS                                                                                                                                                                                                                                                                                    |
| ADMIN-09: retry/rebuild/approve-deliver require two-step confirm with effect description before executing                                                                                                                     | arm + effect + confirm executes                                   | `apps/web/src/admin/routes.test.tsx:256-259` — `click /tentar novamente este job/` → `text /este job volta para a fila.*só este job/` → `click /confirmar/` → `retryJob toHaveBeenCalledWith('job-1')`; `:260-267` — `click /aprovar faixa 1 e entregar pedido/` → `text /marca o pedido inteiro como entregue/` → `click /confirmar/` → `adminApproveAudio toHaveBeenCalledWith('order-9','audio-1')`; `:304-309` — rebuild arm + effect + `adminRebuildAudio toHaveBeenCalledWith('order-9')`                                                   | ✅ PASS — F-04 (approve/rebuild execution part) closed                                                                                                                                                                                                                                     |
| ADMIN-10: audio actions declare per-track vs both-versions scope                                                                                                                                                              | scope labels                                                      | `apps/web/src/admin/routes.test.tsx:260-267` (`button /aprovar faixa 1 e entregar pedido/` + `text /marca o pedido inteiro como entregue/` = per-track action with conjunto effect) + `:304-307` (`text /gera as duas versões do zero.*conjunto das duas versões/`); impl `routes.tsx:626` (`uma faixa`) vs `:642,653` (`conjunto`)                                                                                                                                                                                                               | ✅ PASS                                                                                                                                                                                                                                                                                    |
| ADMIN-11: rebuild declares both versions rebuilt from scratch from latest approved lyrics                                                                                                                                     | rebuild effect text                                               | `apps/web/src/admin/routes.test.tsx:304-309` — `click /reproduzir as 2 versões/` → `text /gera as duas versões do zero.*conjunto das duas versões/` → `click /confirmar/` → `adminRebuildAudio` called; impl `apps/web/src/admin/routes.tsx:651-654` full effect `apaga os áudios atuais e gera as duas versões do zero a partir da letra aprovada mais recente. Afeta o conjunto das duas versões.`                                                                                                                                              | ❌ GAP (partial) — rebuild confirm-execution now asserted, but the asserted regex does not pin the spec-critical substring `a partir da letra aprovada mais recente`; a mutant deleting only that clause would survive the current regex                                                   |
| A11Y-01: at 390×844 `documentElement.scrollWidth === clientWidth` on audited public + admin routes                                                                                                                            | exact equality                                                    | `apps/web/e2e/ui-remodel-qa.spec.ts:161-172` — poll `toEqual({clientWidth:390,scrollWidth:390})` for `/`, `/criar`, checkout; `:270-290` — same poll for `/admin`, `/admin/pedidos`, `/admin/pedidos/qa-internal-1`; `apps/web/e2e/mvp-operable-t7.spec.ts:176-191` — same for `/`, `/criar`, checkout, `/minhas-musicas`                                                                                                                                                                                                                         | ✅ PASS — F-05 (admin overflow part) closed                                                                                                                                                                                                                                                |
| A11Y-02: open mobile menu traps focus (cycles in menu, background `inert`), Tab never reaches covered content                                                                                                                 | dialog + inert + trap                                             | `apps/web/src/components.test.tsx:52-66` — `dialog aria-modal=true`, `main toHaveAttribute('inert')` + Tab wraps to brand link; `apps/web/e2e/mvp-operable-t7.spec.ts:45-68` — `main hasAttribute('inert') toBe(true)`, `dialog :focus count 1` after Tab                                                                                                                                                                                                                                                                                         | ✅ PASS                                                                                                                                                                                                                                                                                    |
| A11Y-03: Escape closes menu, restores focus to menu button                                                                                                                                                                    | close + focus restore                                             | `apps/web/src/components.test.tsx:24-25` — `button /abrir menu/ toHaveFocus()` after Escape; `apps/web/e2e/mvp-operable-t7.spec.ts:37-39` — same; `apps/web/e2e/ui-remodel-qa.spec.ts:178-179` — same                                                                                                                                                                                                                                                                                                                                             | ✅ PASS                                                                                                                                                                                                                                                                                    |
| A11Y-04: accessible names on nav/buttons/progress + `nav`/`main`/`dialog` semantics where applicable                                                                                                                          | names + landmarks                                                 | `apps/web/src/components.test.tsx:52` (`dialog`+`aria-modal`), `apps/web/src/public-form.test.tsx:93` (progressbar name), `apps/web/src/admin/routes.test.tsx:89-91,138-143` (`navigation 'Administração'` + links), `apps/web/e2e/ui-remodel-qa.spec.ts:222-223` (admin nav links) + button-name assertions throughout (`pagar`, `filtrar`, `confirmar`, `salvar história`)                                                                                                                                                                      | ✅ PASS — v1 gap (admin `nav[Administração]` unasserted) now closed                                                                                                                                                                                                                        |
| A11Y-05: interactive targets ≥44×44px on audited surfaces                                                                                                                                                                     | min dimensions                                                    | `apps/web/e2e/mvp-operable-t7.spec.ts:192-206` — CTA `boundingBox >= 44×44`, all visible `criar minha música` links `>= 44`; impl `styles.css:50-51,141,265-269,282-284,294-304,562-571` (`min-height:44px`, `min-width:44px`)                                                                                                                                                                                                                                                                                                                    | ✅ PASS (scoped; impl + E2E agree)                                                                                                                                                                                                                                                         |
| A11Y-06: failed validation associates each error via `aria-describedby`, moves focus to first error, keeps visible focus                                                                                                      | describedby + focus + outline                                     | `apps/web/src/public-form.test.tsx:47-51` — `occasion toHaveFocus()` + `aria-invalid=true` + `HaveAccessibleDescription('Conte a ocasião')`; `:76-80` — all 6 controls `aria-invalid` + describedby message; `apps/web/e2e/mvp-operable-t7.spec.ts:84-101` — `:focus outline solid ≥3px`, contrast ≥3:1                                                                                                                                                                                                                                           | ✅ PASS                                                                                                                                                                                                                                                                                    |
| A11Y-07: keyboard-only journey completes with no trap and coherent order                                                                                                                                                      | full keyboard traversal                                           | `apps/web/e2e/ui-remodel-qa.spec.ts:182-207` — keyboard-only `/criar`: Tab-loop (guard 60) until `salvar história e continuar` reachable (`reachedCta toBe(true)` = no trap), `Enter`, then `label /para quem é a música/ toBeFocused()` (coherent focus into first error)                                                                                                                                                                                                                                                                        | ❌ GAP (partial) — first-step keyboard traversal with no-trap guard now asserted; full jornada (`/criar` → `/criar/letra` → `/criar/checkout`) completed keyboard-only end-to-end is not asserted (no Tab/Enter traversal of letra approval or checkout payment)                           |
| GUARD-01: gates green; feature does not touch `worker.ts`/domain invariants; no secrets/providers/remote claims                                                                                                               | gates + isolation                                                 | Verifier-observed: `format:check` PASS, `lint` 8/8 PASS, `typecheck` 8/8 PASS, web **63/63** (10 files), api **38/38** (5 files), `build` 8/8 PASS, `git diff --check` PASS; E2E **37 tests in 7 files trusted** (files exist, counts match author claim; not re-executed by verifier — see Gate Check); `worker.ts` diff is pre-existing WIP, untouched by feature                                                                                                                                                                               | ✅ PASS (E2E trusted, not re-run)                                                                                                                                                                                                                                                          |

**Status**: ❌ Gaps present — strict spec-anchored **25/31 PASS**. 6 criteria are GAP/partial (all partial, none with zero `file:line` anymore): JOURNEY-03 (3/4 prep bullets), LIB-01 (3/5 fields), ADMIN-01 (context/exit), ADMIN-07 (letra heading), ADMIN-11 (approved-lyrics clause), A11Y-07 (first step only).

---

## Discrimination Sensor

Scratch-only, never `git stash`, never mutated the real tree. Baseline `git status --porcelain=v1` captured before, re-compared after: **identical** (`PORCELAIN-MATCHES-BASELINE`), plus `git diff --check` clean. Files copied to `/tmp/ui-remodel-sensor/`, mutated in place, restored via `cp *.orig`, verified with `diff -q` (`SCRATCH-DISCARDED`).

| Mutation | File:line                               | Description                                                                                                         | Killed?                                                                                                                                                                                                                     |
| -------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S2       | `apps/web/src/pages/public.tsx:553-554` | `paymentState !== undefined && …` → `paymentState === undefined && …` (unavailable never true; disabled-state dead) | ✅ Killed — `src/pages/checkout.test.tsx` 1 failed / 2 passed (the `nasce desabilitado com motivo` case fails: button enabled, `role=status` reason absent). v1 survived web unit (51/51); now killed → F-S2 verified fixed |
| S4       | `apps/web/src/admin/labels.ts:43-48`    | `maskEmail` returns full `${user}@${domain}`                                                                        | ✅ Killed — `src/admin/routes.test.tsx` fails (`toBe 'a•••@example.test'` and full-email-absent assertions fail)                                                                                                            |
| S5       | `apps/web/src/components.tsx:187`       | `journeyLabels[step-1]` → `journeyLabels[step % 5]` (step names rotated; values unchanged)                          | ✅ Killed — `src/components.test.tsx` 5 failed / 4 passed (all 5 `it.each` name-exact cases fail). Corroborates F-01: v1's weak regex would have survived this; the new name-exact assertion kills it                       |

Prior v1 mutations S1 (`aria-valuenow` flip → killed by `public-form.test.tsx:96`) and S3 (removed `inert` → killed by `components.test.tsx:55`) address unchanged code and were not re-run; the three mutations above target the NEW fix code.

**Sensor depth**: targeted behavior-level mutations on highest-risk new fix code (payment gate, PII masking, journey names).
**Sensor outcome**: 3/3 killed at unit level on NEW fix code (S2 payment-gate, S4 maskEmail, S5 journey-name flip) — F-S2 hole closed; PORCELAIN-MATCHES-BASELINE after sensor.

---

## Interactive UAT Results (if performed)

Not performed — out of scope for this read-only verification pass (no human session; Browser QA evidence in `.specs/features/ui-remodel/local-validation.md` and `apps/web/e2e/ui-remodel-qa.spec.ts` stands as the automated substitute).

---

## Code Quality

| Principle                                                                    | Status                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Minimum code                                                                 | ✅ — additive changes on existing files/components; no new frontend/design-system (per out-of-scope)                                                                                                   |
| Surgical changes                                                             | ✅ — feature files only; `worker.ts`/domain invariants untouched by feature                                                                                                                            |
| No scope creep                                                               | ✅ — no unrelated behavior changes found in feature files                                                                                                                                              |
| Matches patterns                                                             | ✅ — RTL/Vitest co-located, `fastify.inject`, Playwright route-mocks follow existing conventions                                                                                                       |
| Spec-anchored outcome check (asserted values match spec)                     | ❌ — 6/31 criteria lack fully value-exact assertions (see table; all are partial-coverage residuals, listed as ranked gaps below)                                                                      |
| Per-layer Coverage Expectation met (domain 1:1 ACs; routes happy+edge+error) | ⚠️ — web unit now has `Checkout` (3 tests), admin shell/empty/error/rebuild/approve, landing-demo via E2E; remaining holes: `MyOrders` date/progress unit, `Letra aprovada` heading, full keyboard E2E |
| Every test maps to a spec requirement - no unclaimed tests                   | ✅ — all new/changed tests trace to JOURNEY/FORM/CHECK/ADMIN/A11Y ACs or their Done-when criteria                                                                                                      |
| Documented guidelines followed                                               | ✅ — `AGENTS.md` (Corepack/pnpm, strict TS/ESM, domain guards via `assertTransition`, UTC/cents, no secret logging)                                                                                    |

Spot-check (one story — P1 Checkout): E2E + new RTL assert product + honoree/title + price + contents + next-step + redirect + disabled-with-reason + blocked-launch sentence — the story's core trust content is now value-exact verified (`checkout.test.tsx:53-89`, `ui-remodel-qa.spec.ts:148-156`). Contrast with P1 Biblioteca (`MyOrders`): title/occasion/next-action asserted via E2E only, date/progress/no-ID unasserted and unclaimed by any fix task.

---

## Edge Cases

From `spec.md` Edge Cases (each checked against impl + test):

- [x] Local storage blocked → in-memory fallback + announced non-persistence: impl `use-draft.ts` try/catch with `status='error'` copy (`public.tsx:152-154`); test `use-draft.test.tsx:18-29` tolerates `removeItem` throw.
- [x] Missing/invalid order access → readable error + return home, no PII: `PageError` (`public.tsx:1032-1044`) + E2E `mvp-flows.spec.ts:126-134` (`alert` + `Voltar ao início` → `/`).
- [x] Missing lyrics in `lyrics_ready` → inconsistent, no edit offered: `public.tsx:339-340` + E2E `mvp-operable-t6.spec.ts:140-163` (alert + zero main buttons).
- [x] Partial delivery (`delivered` without 2 completed variants) → inconsistent, no player: `order-journey.ts:62-63` + E2E `mvp-operable-t6.spec.ts:172-196` (alert → 5× Concluído + 2 audios only when complete).
- [x] Overview/list API failure → error + retry, no invented metrics: impl error copy (`routes.tsx:155-167`, `:359`) + unit `routes.test.tsx:146-170` (list empty + list error + overview error, no estimated totals). **Closed since v1.**
- [x] Missing `MERCADO_PAGO_ACCESS_TOKEN` in production → checkout born disabled with reason: impl `app.ts:901-905` + `public.tsx:553-612`; unit `checkout.test.tsx:71-80` + E2E `mvp-operable-t5.spec.ts:126-141` (unavailable shape) covers the branch.
- [x] Rebuild declares both versions from latest approved lyrics: impl text present (`routes.tsx:651-654`) + unit renders/confirms it (`routes.test.tsx:304-309`). **Implemented; assertion pins `duas versões do zero` + `conjunto` but not the middle clause `letra aprovada mais recente` — residual ADMIN-11 gap.**

---

## Gate Check

- **Gate command**: `pnpm format:check` + `pnpm lint` + `pnpm typecheck` + `pnpm --filter @resenha/web test --run` + `pnpm --filter @resenha/api test` + `pnpm build` + `git diff --check` (E2E trusted via file existence + count, not re-executed — browser run would need preview server + screenshots)
- **Gate outcome**: all verifier-executed gates green, 0 failed, 0 skipped
  - `pnpm format:check`: PASS (Prettier, whole workspace)
  - `pnpm lint`: PASS (8/8 Turbo tasks)
  - `pnpm typecheck`: PASS (8/8 Turbo tasks)
  - web unit: **63/63 passed** (10 files: incl. `components.test.tsx` 9 — 5 of which are the new `it.each` journey cases, `public-form.test.tsx` 4, `pages/checkout.test.tsx` 3, `admin/routes.test.tsx` 9)
  - api integration: **38/38 passed** (5 files, incl. `flow.test.ts` 21 — needs live PostgreSQL at `localhost:5433`, present)
  - E2E Playwright: **trusted 37 tests in 7 files** (`landing` 1 + `mvp-flows` 8 + `mvp-operable-t5` 4 + `mvp-operable-t6` 8 + `mvp-operable-t7` 9 + `p2-cover` 2 + `ui-remodel-qa` 5 = 37; verifier confirmed each file exists and `grep -c` counts sum to 37; did not execute browser run)
  - `pnpm build`: PASS (8/8 Turbo tasks; web entry 71,060 bytes < 500,000 ceiling)
  - `git diff --check`: PASS (no whitespace errors)
- **Test count before feature**: not recorded by author (no baseline committed); verifier confirms current totals above and that the feature diff only **adds/extends** tests (no deletions except label renames `gerar minha letra` → `salvar história e continuar` tracking the spec-required CTA).
- **Delta since v1**: web unit 51→63 (+12: +5 journey `it.each` net of prior 4, +4 admin shell/order/empty/error/rebuild/approve net of prior 5, +3 new `checkout.test.tsx`); api 38→38; E2E 34→37 trusted (+3 new QA tests: keyboard-only, demo honesty, admin overflow).
- **Skipped tests**: none.
- **Failures**: none.

---

## Fix Plans (residual gaps)

### G-01: Assert 4th prep bullet (JOURNEY-03)

- **Root cause**: QA asserts 3/4 prep bullets; the what-generated bullet (`public.tsx:445`) has no assertion.
- **Fix task**: add `getByText(/transformar sua história salva em uma letra revisável/i)` to `ui-remodel-qa.spec.ts:141-145` (or RTL equivalent).
- **Priority**: Minor (one line; heading + other 3 bullets already pinned).

### G-02: Cover MyOrders date/progress/no-ID (LIB-01)

- **Root cause**: E2E stops at title/occasion/next-action; `Criada em`, `Progresso`, no-tech-ID-as-title (`public.tsx:858-871`) unasserted; no unit test. Not in v1 F-list, never claimed.
- **Fix task**: extend `mvp-flows.spec.ts:171-175` with `text /criada em/`, `text /progresso/`, and `not.toContainText(/order-mine-123/)` as title; or add RTL `MyOrders` test with mocked `api.getOrder`.
- **Priority**: Major (only criterion with zero fix-task coverage; user-visible library trust).

### G-03: Assert admin context + exit (ADMIN-01)

- **Root cause**: nav persistence on 3 routes asserted; `Operação · dados sintéticos` context and `Sair` exit (`shell.tsx:42`, `:7-16`) unasserted.
- **Fix task**: add `getByText(/operação · dados sintéticos/i)` + `getByRole('button',{name:/sair/i})` to `routes.test.tsx:98-144` loop.
- **Priority**: Minor (two assertions).

### G-04: Assert letra section + approved-lyrics clause (ADMIN-07/11)

- **Root cause**: detail asserts 5/6 headings + no-JSON; `Letra aprovada` heading (`routes.tsx:554`) unasserted. Rebuild regex pins ends but not middle clause `a partir da letra aprovada mais recente`.
- **Fix task**: add `getByRole('heading',{name:/letra aprovada/i})` to `routes.test.tsx:246-255`; tighten rebuild assertion to `/a partir da letra aprovada mais recente/i`.
- **Priority**: Minor (two assertions; impl already contains both strings).

### G-05: Extend keyboard traversal beyond first step (A11Y-07)

- **Root cause**: QA proves no-trap + coherent order for `/criar` only; letra approval + checkout payment keyboard-only completion unasserted.
- **Fix task**: extend `ui-remodel-qa.spec.ts:182-207` to continue via keyboard through `/criar/letra` (generate → approve) and `/criar/checkout` (pay), or document the criterion as first-step-scoped.
- **Priority**: Minor (a11y hardening; menu containment + focus-visible + describedby are strong).

---

## Requirement Traceability Update

Spec.md statuses are author-owned (`Pending`); the verifier does not mutate the spec. Recommended updates on fix acceptance:

| Requirement | Previous Status | New Status                                  |
| ----------- | --------------- | ------------------------------------------- |
| JOURNEY-01  | Pending         | ✅ Verified (F-01)                          |
| JOURNEY-02  | Pending         | ✅ Verified                                 |
| JOURNEY-03  | Pending         | ❌ Needs Fix (G-01: 1 bullet)               |
| JOURNEY-04  | Pending         | ✅ Verified                                 |
| LIB-01      | Pending         | ❌ Needs Fix (G-02)                         |
| FORM-01     | Pending         | ✅ Verified                                 |
| FORM-02     | Pending         | ✅ Verified                                 |
| DEMO-01     | Pending         | ✅ Verified (F-03)                          |
| CHECK-01    | Pending         | ✅ Verified (F-02)                          |
| CHECK-02    | Pending         | ✅ Verified                                 |
| CHECK-03    | Pending         | ✅ Verified (F-S2)                          |
| CHECK-04    | Pending         | ✅ Verified (F-02)                          |
| ADMIN-01    | Pending         | ❌ Needs Fix (G-03: context/exit)           |
| ADMIN-02    | Pending         | ✅ Verified                                 |
| ADMIN-03    | Pending         | ✅ Verified                                 |
| ADMIN-04    | Pending         | ✅ Verified                                 |
| ADMIN-05    | Pending         | ✅ Verified (F-04)                          |
| ADMIN-06    | Pending         | ✅ Verified                                 |
| ADMIN-07    | Pending         | ❌ Needs Fix (G-04: letra heading)          |
| ADMIN-08    | Pending         | ✅ Verified                                 |
| ADMIN-09    | Pending         | ✅ Verified (F-04)                          |
| ADMIN-10    | Pending         | ✅ Verified                                 |
| ADMIN-11    | Pending         | ❌ Needs Fix (G-04: approved-lyrics clause) |
| A11Y-01     | Pending         | ✅ Verified (F-05)                          |
| A11Y-02     | Pending         | ✅ Verified                                 |
| A11Y-03     | Pending         | ✅ Verified                                 |
| A11Y-04     | Pending         | ✅ Verified                                 |
| A11Y-05     | Pending         | ✅ Verified                                 |
| A11Y-06     | Pending         | ✅ Verified                                 |
| A11Y-07     | Pending         | ❌ Needs Fix (G-05: full journey)           |
| GUARD-01    | Pending         | ✅ Verified (E2E trusted)                   |

---

## Summary

**Overall**: ❌ Not Ready (FAIL — 6 partial residuals; max 3 fix→re-verify iterations not yet exhausted)

**Spec-anchored check**: 25/31 ACs matched with value-exact evidence (up from 16/31 in v1). All 6 gaps are partial (no zero-`file:line` criterion remains): JOURNEY-03 (3/4 prep bullets), LIB-01 (3/5 library fields), ADMIN-01 (nav yes, context/exit no), ADMIN-07 (5/6 sections), ADMIN-11 (rebuild ends pinned, middle clause not), A11Y-07 (first step only).
**Sensor**: 3/3 killed at unit level on NEW fix code (S2 payment-gate, S4 maskEmail, S5 journey-name flip) — F-S2 hole closed; PORCELAIN-MATCHES-BASELINE after sensor.
**Gate**: format PASS, lint 8/8, typecheck 8/8, web 63/63, api 38/38, build 8/8, `git diff --check` PASS; E2E 37 tests in 7 files trusted via existence + grep count (not browser-executed).

**What works**: five-step journey with name-exact progress (1-5) + faithful CTA + polling generation; autocomplete + draft controls; landing-demo honesty with audio-count-0; checkout trust block fully pinned (product/title/price/contents/next-step + redirect + disabled-with-reason unit + blocked-launch sentence); PT-BR admin labels with masked PII; alerts-before-cards DOM order; server aggregates with SQL-verified totals; explicit filters with page-reset + pagination totals + empty/error states; retry/approve/rebuild two-step confirms all executing; contained mobile menu with `inert` + Escape restore; 44px targets and error association; public + admin 390×844 overflow equality.

**Issues found**: six partial residuals listed as G-01..G-05 above — each is a 1-2 assertion gap, not a behavior hole (impl contains the required strings in every case; verified by reading `public.tsx`, `routes.tsx`, `shell.tsx`). The only criterion with no fix-task lineage is LIB-01 (date/progress/no-ID).

**Next steps**: implement G-01…G-05 (5 micro-assertions + 1 small `MyOrders` extension), then re-run this verification. Do not declare the feature done, and do not push/deploy, on the current evidence.
